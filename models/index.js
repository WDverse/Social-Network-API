// Import the 'User' module from './User.js'.
const User = require('./User');

// Import the 'Thought' module from './Thought.js'.
const Thought = require('./Thought');

// Import the 'Reaction' module from './Reaction.js'.
const Reaction = require('./Reaction');

// Export an object containing references to the imported modules.
module.exports = { User, Thought, Reaction };
